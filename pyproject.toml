[project]
name = "ve-optimizer"
version = "0.1.0"
description = "A genetic optimizer for visual effects in HarmonyOS"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "deap>=1.4.3",
    "matplotlib>=3.10.7",
]
